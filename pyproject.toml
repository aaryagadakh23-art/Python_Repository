
[project]
name = "sample-project"
version = "0.1.0"
description = "FastAPI app for Vercel deployment"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "backend.app.main:app"
